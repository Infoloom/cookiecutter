# Base TCT project template

initialize this template with the command:

```
django-admin startproject --template=https://github.com/Infoloom/tct-cookiecutter/archive/master.zip --extension=py,json PROJECTNAME .
```
